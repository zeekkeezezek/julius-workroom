# JULIUS WORKROOM v1.3.6 検証記録

検証日: 2026-09-03

## 対象と方法

EXERCISE新規記録の台詞拡張と件数別分岐。v1.3.5との静的差分確認、および本体から読み出した関数を実行する隔離環境で自動検証した。DOM・保存・音声の呼び出し先はテスト用の代替を使用し、実際のlocalStorage・Firestoreには書き込んでいない。

## 確認済み

- index.htmlの全インラインJavaScript、cloud-sync.js、firebase-config.js、service-worker.jsの構文正常
- DOM ID 190個に重複なし。固定ID参照の欠落なし
- 変更を新しい台詞定義、2つの補助関数、新規記録からの台詞呼び出し、バージョン表示へ限定。それらを元に戻したHTMLがv1.3.5と完全一致（改行コードは正規化して比較）
- Firebase設定、Cloud Sync、manifest、全画像・アイコンなど18ファイルがv1.3.5とバイト単位で一致
- PWAの全App Shellとmanifestのアイコンが存在。Service Workerはキャッシュ名とスクリプトのバージョン以外に変更なし
- 既存の台詞36分類が完全一致。新規は2件用7種、3件用8種、4件用8種、5件以上用8種、60分補助5種、90分補助5種の計41種
- 1〜6件目、100件目の件数分岐が正しく、5件以上で「二件」「三件」「四件」と言わない
- 同じ種目を繰り返しても記録件数として扱う。種目数を決めつける文面なし
- 実際のrecordExercise関数で1〜6件を順に追加し、保存→描画→運動SE1回→対応台詞の順序を確認
- プランク10回＋5分＋10分＋20分で4件・35分・4件用台詞。旧時間型プランクの分数は従来どおり加算
- 目標120分を30分＋30分＋60分の3件目で初達成すると週間達成を優先。次の1分の追加では通常判定に復帰
- 件数3以上、59.99／60／89.99／90分、抽選値25%／35%前後の境界を確認。1〜2件では長時間補助に分岐しない
- 長時間補助は必ず出るわけではなく、件数別台詞も引き続き出る。週間初達成では抽選処理より先に達成台詞を返す
- 前日分を当日件数・分数へ含めず、記録削除後も現在のexerciseLogsから再計算する
- 名前空欄では保存・SE・台詞なし。保存が例外終了した場合も成功SE・台詞なし
- 各新規分類で24回ずつ選択し、同分類の直近3台詞を回避する既存ロジックを確認
- 当日にWORK記録がある1件目のexerciseBothへの既存切り替えを維持
- schema version 10、localStorageキー、JSON、同期競合処理、revision・hash・writerId、CLOUD DATA SIZE、UI SE音量・音色、INBOXに変更なし。新規保存フィールドなし

## 実環境での最終確認

この記録は自動検証の結果であり、PC／iPhone実機での表示・音声・Cloud Syncの確認結果ではない。公開反映後は、既存データとJSONバックアップを確認し、同期テスト用の機能でPC → iPhone → PCを確認する。

台詞の件数テストは本番に架空の運動履歴を混ぜず、未ログインのローカル確認環境で実施する。操作手順はREADME.mdの「v1.3.6台詞確認」を参照。

# v1.5.1 検証結果（2026-09-23）

## 合格
- JavaScript構文、DOM IDの重複と参照先、PWAキャッシュ対象ファイル。
- schema 1～12の移行・JSON往復、旧データ原本退避、壊れた保存データ／容量不足時の停止。
- PCのカード管理、タイマー開始／一時停止／終了／取消、通常ログ編集・削除。
- 手動追加：今日30分、昨日45分、1週間前20分、空メモ／メモあり、同名別IDの独立集計。
- activityの入力日付、日別時間、HOME、カード累計、CALENDAR、LOG、lastWorkedAt。
- 手動記録の編集・削除で再集計し、最終作業日も再計算。
- 空欄／0／負数／小数／空日時の拒否、保存失敗時のメモリと保存済みデータの維持。
- 同一フォーム連打で1件だけ保存。表示中に対象がアーカイブされた場合は保存拒否。
- 手動記録中の既存タイマーを維持。成功時に既存の同期通知を呼ぶことを確認。
- 手動ログのJSON export/import、schema 12を維持。
- SE OFFでは音なし、ONでは既存WORK完了音。新反応7本から表示。
- 320px／390pxで縦並び入力、横はみ出しなし。390pxでカード管理から保存まで操作。
- スマホ下部5項目とCALENDAR切替、PCの2カレンダー、EXERCISE既存操作。
- Service Workerのオフライン再起動。
- 模擬Firestoreで初回比較、競合拒否、安全コピー、書込中の再編集、2端末往復、900KB上限。
- schema.js／cloud-sync.js／firebase-config.js／立ち絵はv1.5.0から変更なし。

## 範囲
隔離したEdgeのテストデータで実施。PC／スマホ幅のスクリーンショットも確認。
本番Firestore・実機iPhoneの手動ログ往復は未実施。公開後にREADMEの手順で確認する。
本番のlocalStorageやクラウド記録の初期化・テスト記録追加は行っていない。

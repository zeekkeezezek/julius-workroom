# v1.5.0 ローカル検証記録

検証日: 2026-09-19。公開操作・本番Firestoreへの書き込みは行っていない。

## 合格した項目

- HTML内JavaScriptと全JSファイルの構文、DOM ID重複・参照先、PWA参照ファイルの存在。
- Firebase設定と6立ち絵のファイル内容がv1.4.1から変わっていないこと。
- v1～v11のテストJSON→v12、v12再読み込みの決定的な変換。原本を変更しないこと。
- 同名別ID、完成済み作業、参照先のない過去ログ、小さな一歩、INBOX、運動、設定の維持。
- 実行中・一時停止中・アーカイブされた作業のタイマー引き継ぎ。参照先欠損時だけ取消。
- Edgeの隔離されたテスト用ブラウザで、追加→▶開始→停止／再開→空メモ記録、名前変更、集計、アーカイブ・復元。
- タイマー満了時のモーダル、記録の二重追加防止、取消確認。満了の検出だけではクラウド用データを書き換えないため、両端末の同時満了による不要な競合を避ける。
- INBOX→作業変換、保存失敗時のINBOX保持。小さな一歩の独立記録。
- EXERCISE全6種、回数型プランク、週間目標、HOME運動表示、WORKログ編集。
- PCカレンダー縦2面、スマホ幅での切替式、下部ナビ、横はみ出しなし。
- 通常JSONがversion 12で旧categories／bodyDaysを含まないこと。
- 旧JSONの実際のFileReader読み込み、複数回移行した際の原本コピー保持。
- 不正localStorage／移行バックアップ容量不足時は、元データを初期化せず停止すること。
- Service Worker登録、新JSのキャッシュ、通信なしでの再読み込み。
- Firebase通信を模した隔離テストで、v11クラウドとv12ローカルが同内容なら再選択不要。
- 前回同期済みハッシュの安全な変換、新しいクラウドの受信、受信後のタイマー更新。
- 初回クラウド空・内容不一致時の明示選択、採用前バックアップ、revision競合による上書き停止。
- 保存中の追加変更を次の保存へ引き継ぐこと。
- 模擬2端末のPC→スマホ→PC往復、900KB超過停止、不明schema・保存エラー時のデータ保持。
- 配布ZIPの全収録ファイルをソースとSHA-256で照合。index.htmlはZIP直下に配置。

## テスト環境と限界

画面テストはヘッドレスEdgeの1440px／390px幅で実行し、画像でも確認した。
Firebase SDKへの実通信は遮断し、テスト記録だけを独立したブラウザ保存領域へ投入した。
模擬Firestoreはrevision競合などアプリ側の処理検証であり、実サービスへの疎通試験ではない。

未実施: 本番Googleポップアップ認証、実機iPhone／Safari PWA、実Firestoreの往復同期、端末スピーカーでのSE聴感確認。
公開後にREADMEの同期テストを行い、両端末をv1.5.0へ揃えてから本運用を再開する。
